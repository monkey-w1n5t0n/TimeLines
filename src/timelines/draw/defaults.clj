#_(ns timelines.draw.defaults
  (:require [timelines.draw.macros :refer [defrecord-graphic]])
  (:import
   [org.jetbrains.skija
    ;; BackendRenderTarget
    ;; Canvas
    ;; ColorSpace
    ;; DirectContext
    ;; FramebufferFormat
    Paint PaintMode
    Rect
    ;; Surface
    ;; SurfaceColorFormat
    ;; SurfaceOrigin
    ]
   )
  )


